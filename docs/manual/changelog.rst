.. This page is generated from the repository-root CHANGELOG.md, which is
   the single source of release history. Edit that file, not this one.
   `make release` rolls its [Unreleased] section into the new version, so
   the published manual follows a release without a second edit.

.. include:: ../../CHANGELOG.md
   :parser: myst_parser.sphinx_
