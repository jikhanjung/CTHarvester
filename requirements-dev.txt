# CTHarvester Development Dependencies
#
# NOTE: This file lists version RANGES and is kept for backward compatibility.
# It is NOT what CI installs.
#
#   Declaration (ranges)  -> pyproject.toml  [project.optional-dependencies].dev
#   Resolution (pinned)   -> requirements-dev-<os>.lock  (exact versions + hashes)
#
# The locks are per-platform (linux / windows / macos); `make install-dev` picks
# the one matching the machine. Regenerate them all with `make lock`.

-r requirements.txt

# Testing
pytest>=9.0.0,<10.0.0
pytest-cov>=4.1.0,<8.0.0
pytest-qt>=4.2.0,<5.0.0
hypothesis>=6.0.0,<7.0.0

# Code Quality (ruff replaces black + isort + flake8 + pyupgrade + pylint;
# pinned exactly so a newer release cannot reformat the tree unannounced)
ruff==0.16.1

# Type Checking
mypy>=1.7.0,<2.0.0
types-PyYAML>=6.0.0,<7.0.0

# Profiling
memory-profiler>=0.61.0,<1.0.0

# Documentation toolchain, so `make docs` and `make docs-watch` work out of the
# box. Declared in pyproject.toml's `docs` extra, which the dev lockfiles are
# compiled with; docs/manual/requirements.txt is the narrower set CI uses.
-r docs/manual/requirements.txt
sphinx-autobuild>=2024.2.4
